define command {
  command_name    check_http_url
  command_line    /usr/lib/nagios/plugins/check_http -H '$ARG1$' -4 -u '$ARG2$'
}

define command {
  command_name    check_icmp
  command_line    sudo /usr/lib/nagios/plugins/check_icmp '$ARG1$'
}

